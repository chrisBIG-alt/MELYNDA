package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Melynda OS Immersive Cyber-Slate Digital Color Tokens
val CyberCyan = Color(0xFF00E5FF)       // Primary action items, glowing status indicators
val SoftCyan = Color(0xFF80DEEA)        // Readable highlights and data labels
val DeepSpace = Color(0xFF070B14)       // Primary window canvas backdrop (99% black with blue hint)
val SlateOverlay = Color(0xFF111726)     // Custom card container, translucent depth
val ActiveGreen = Color(0xFF00E676)     // Wake word listening, active sandbox, success state
val AlertOrange = Color(0xFFFF9100)     // Sandbox intervention, warnings, critics
val MutedText = Color(0xFF78909C)       // Terminal comments, auxiliary metadata
val TerminalStdout = Color(0xFFECEFF1)  // Clean console output text
